{% extends 'front_office_dashboard/base.html.twig' %}

{% block title %}Tournois{% endblock %}

{% block banner %}
  <!--================= Banner Section Start Here =================-->
  <div class="banner banner1 position-relative">
    <div class="inner-page-banner banner-bg overlay-dark">
      <div class="container">
        <div class="banner-content text-center py-5">
          <div class="page-path mb-3">
            <ul class="list-inline mb-0">
              <li class="list-inline-item"><a class="home-page-link text-white opacity-75 text-decoration-none" href="{{ path('app_home1') }}">Home</a></li>
              <li class="list-inline-item text-white opacity-75">/</li>
              <li class="list-inline-item"><a class="current-page text-white fw-bold" href="#">Tournois</a></li>
            </ul>
          </div>
          <h1 class="banner-heading text-uppercase text-white display-4 fw-bold mb-0">{{ tournoi.nom }} MATCHES</h1>
        </div>
      </div>
    </div>
  </div>
  <!--================= Banner Section End Here =================-->
{% endblock %}

{% block content %}
  <div class="container py-5">
    <h2 class="mb-4">Matches for “{{ tournoi.nom }}”</h2>

    {% if tournoi.matches|length %}
      <ul class="list-group">
        {% for match in tournoi.matches %}
          <li class="list-group-item d-flex justify-content-between align-items-center">
            <span>
              {{ match.teamA.nom }} vs {{ match.teamB.nom }}
              <small class="text-muted">
                on {{ match.matchTime|date('M d, Y – h:i A') }}
              </small>
            </span>
            <a href="{{ path('app_front_match_show', { id: match.id }) }}"
               class="btn btn-sm btn-outline-secondary">
              Details
            </a>
          </li>
        {% endfor %}
      </ul>
    {% else %}
      <div class="alert alert-info">
        No matches scheduled for this tournament.
      </div>
    {% endif %}
  </div>
 {% include 'chat/_widget.html.twig' with {
   
} %}
{% endblock %}
